sudo: false

language: python
os: windows

services:
- docker

script:
- make image

before_deploy:
- docker login -u $DOCKER_USER -p $DOCKER_PASS

deploy:
- provider: script
  script: make push
  on:
    branch: master
    tags: true
